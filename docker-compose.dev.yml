services:
  pokt-portal-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      # Required Environment Variables
      - SESSION_SECRET=your_session_secret_here_make_it_long_and_random
      - AUTH0_CLIENT_ID=your_auth0_client_id_here
      - AUTH0_CLIENT_SECRET=your_auth0_client_secret_here
      - AUTH0_DOMAIN=your_auth0_domain_here
      - AUTH0_AUDIENCE=your_auth0_audience_here
      - AUTH0_SCOPE=openid profile email
      - AUTH0_SECRET=your_auth0_secret_here
      - AUTH0_BASE_URL=http://localhost:3000
      - AUTH0_ISSUER_BASE_URL=https://your-domain.auth0.com
      # Optional Environment Variables
      # - STRIPE_SECRET_KEY=your_stripe_secret_key
      # - STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret
      # - STRIPE_PRICE_ID=your_stripe_price_id
      # - NOVU_API_KEY=your_novu_api_key
      # - NOVU_APP_ID=your_novu_app_id
      # - UPSTASH_REDIS_REST_URL=your_upstash_redis_url
      # - UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token
      # - VERCEL_KV_REST_API_URL=your_vercel_kv_url
      # - VERCEL_KV_REST_API_TOKEN=your_vercel_kv_token
      # - VERCEL_KV_REST_API_READ_ONLY_TOKEN=your_vercel_kv_readonly_token
      # - MAILJET_API_KEY=your_mailjet_api_key
      # - MAILJET_API_SECRET=your_mailjet_api_secret
      # - PORTAL_BACKEND_URL=https://backend.staging.portal.pokt.network
      # - PORTAL_GRAPHQL_URL=https://backend.staging.portal.pokt.network/graphql
      # - SENTRY_DSN=your_sentry_dsn_here
      # - FLAG_MAINTENANCE_MODE=false
      # - FLAG_STRIPE_PAYMENT=true
                        # - ADMIN_EMAIL=admin@pokt.ia
                  # - ADMIN_PASSWORD=your_admin_password_here
                  # - ADMIN_KEY=your_admin_key_here
                  # Client Environment Variables
                  # - NOVU_APP_IDENTIFIER=your_novu_app_identifier_here
                  # - PORTAL_API_URL=https://backend.staging.portal.pokt.network
                  # - RELAY_METER_API_URL=your_relay_meter_api_url_here
                  # - GOOGLE_ANALYTICS_ID=your_google_analytics_id_here
                  # - FLAG_ENTERPRISE=false
                  # - FLAG_INFLUX_RELAY_ERROR=false
                  # - FLAG_LEGACY_MESSAGING=false
                  # - FLAG_MAINTENANCE_MODE_DASHBOARD=false
                  # - FLAG_MULTI_LANGUAGE=false
                  # - FLAG_ANNOUNCEMENT_ALERT=false
                  # - ANNOUNCEMENT_ALERT_TITLE=your_announcement_title_here
                  # - ANNOUNCEMENT_ALERT_BODY=your_announcement_body_here
                  # - GODMODE_ACCOUNTS=your_godmode_accounts_here
                  # - VERCEL_ENV=development
                  # - DOCS_STATUS=your_docs_status_here
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    command: ["pnpm", "dev"] 